// const path = require('path')
// const DojoWebpackPlugin = require('dojo-webpack-plugin')
// const webpack = require('webpack')

/*
module.exports = {
  configureWebpack: {
    plugins: [
      // new DojoWebpackPlugin({
      //   loaderConfig: require('./src/amdmodules/loaderConfig'),
      //   // environment: {dojoRoot: "release"},	// used at run time for non-packed resources (e.g. blank.gif)
			// 	buildEnvironment: {dojoRoot: "node_modules"}, // used at build time
			// 	locales: ["en"],
			// 	// noConsole: true,
      //   loader: path.join(__dirname, "./release/dojo/dojo.js")
      // }),
      // new webpack.NormalModuleReplacementPlugin(/^dojo\/text!/, function(data) {
      //   data.request = data.request.replace(/^dojo\/text!/, "!!raw-loader!");
      // }),
    ],
  },
  // chainWebpack: config => {
  //   console.log(config.module.rule('js').test(/amdmodules/).plugin)
  //   config.module
  //     .rule('js')
  //     .test(/amdmodules\//)
  //     .loader(path.join(__dirname, "./release/dojo/dojo.js"))
  //     .end()
  // }
}
*/